# 此目录下的这个 spring.factories 文件(文件名称不能改，且目录不能改，必须在这个 resource 目录下的 META-INF 文件夹下配置 spring.factories 文件)该文件只是针对自定义属性配置使用的，自动装配属性的，当然还有一些其他的配置，比如过滤器什么的，注意这个文件是有提示功能的。该文件只需要配置好自定义的属性，然后在这里指定需要自定义自动配置类的全包名类路径，接着只要没问题，打包编译在 target 包下的 classes/META-INF 文件夹就会出现有关自定义属性的一些配置文件，而且还可以在本模块下，在 application.yml 文件下，就会出现提示功能；这比那种打包编译，然后拷贝 target 包下的 classes/META-INF 文件夹到 resource 目录高效很多，而且在安装之后，其他模块引入本模块之后还不会出现两个自定义属性的功能，也就不需要再手动删除 resource 目录下的 spring-configuration-metadata.json 文件。这个 spring.factories 文件当然还有一些其他的相关配置，具体看官网。有关配置自定义属性出现提示的方法，还有其他的方式，具体见 application.yml 文件，不过最好推荐这种，因为无需手动拷贝，还不会在安装的时候再删除对应 resource 目录下的 spring-configuration-metadata.json 文件.
org.springframework.boot.autoconfigure.EnableAutoConfiguration=cn.example.binapi.sdk.ClientConfig